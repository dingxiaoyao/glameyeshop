# Ignore macOS metadata and local environment files
.DS_Store
.env
vendor/
node_modules/
*.log
*.cache

# Local-only deployment helper
setup-deploy.sh

# User-uploaded files - persist on server, never in git
uploads/
!uploads/.gitkeep
